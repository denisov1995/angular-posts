import { PostColorDirective } from './post-color.directive';

describe('PostColorDirective', () => {
  it('should create an instance', () => {
    const directive = new PostColorDirective();
    expect(directive).toBeTruthy();
  });
});
